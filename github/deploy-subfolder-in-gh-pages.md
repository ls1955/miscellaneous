```
# Suppose subfolder is dist/
1. Ensure subfolder is not ignore by .gitignore
2. Ensure subfolder is commited and pushed
3. git subtree push --prefix dist origin gh-pages
4. In github pages, built-from gh-pages branch
```