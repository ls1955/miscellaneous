```
try:
    1. `sudo apt install libffi-dev` #ubuntu
    2. `dpkg -L libffi-dev`
    3. `sudo ldcondig`
    4. `cd Python/directory`
    5. `make`
    6. `sudo make install`
except Life:
    accept the fate
```
